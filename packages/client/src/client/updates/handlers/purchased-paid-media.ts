import { APIPurchasedPaidMediaUpdate } from '@telescript/api-types';
import { Client } from '../../Client';

export function purchased_paid_media(_client: Client, _update: APIPurchasedPaidMediaUpdate) {}
